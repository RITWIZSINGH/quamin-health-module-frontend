import 'package:flutter/widgets.dart';

class GeneratePlanTab extends StatelessWidget {
  const GeneratePlanTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}